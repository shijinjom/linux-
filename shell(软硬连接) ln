ln 连接 相当于快捷方式
   
   软连接
   ln -s aa/stdio_ext.h  std   给a文件夹下的文件创建快捷方式  为std  如果是相对路径 如果移动 快捷方式名，则在其他的地方不能使用，如果是
                               绝对路径则没有问题
   
   硬链接
    
    ln aa/stdio_ext.h sts    创建出来的硬链接sts  ，sts stdio_ext.h 使用同一个内容控件
                             相当于C++中的引用，改变其中一个值，则另外一个也改变
